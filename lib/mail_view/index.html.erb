<!DOCTYPE html>
<html lang="es">
  <head>

    <meta charset="utf-8">
    <title><%= MailView::EXTENDED_TITLE %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="<%= MailView::TITLE %>">
    <meta name="name" content="<%= MailView::TITLE %>">
    <meta name="robots" content="index, no-follow">

    <style type="text/css">
      header h1 {
        background: dimgrey;
        color: #ffffff;
        font-family: sans-serif;
        margin: -10px -10px 0 -10px;
        padding: 10px;
        text-align: center;
      }
      .content {
        font-family: sans-serif;
        margin: auto;
        width: 50%;
      }
      .content h3 {
        background-color: #A3A3A3;
        border-radius: 5px 5px 0 0;
        border: 1px solid #a3a3a3;
        color: white;
        margin: 5px 0 0 0;
        padding: 10px;
        text-transform: capitalize;
      }
      .content ol {
        /*background-color: #f5faff;*/
        border: 1px solid #a3a3a3;
        border-radius: 0 0 5px 5px;
        margin: 0 0 5px 0;
        padding-bottom: 10px
      }
      .content ol li {
        margin-top: 5px;
      }
      .content ol li a {
        color: black;
        text-decoration: none;
        text-transform: capitalize;
      }
      .content ol li a:visited {
        color: #ff6900;
      }
      .content ol li a:hover {
        color: darkorange;
        text-decoration: none;
      }
    </style>
  </head>

  <body>
    <header>
      <h1>Lista de emails</h1>
    </header>
    <div class="content">
      <% links.each_pair do |group, links| %>
        <h3>
          <%= group %>
        </h3>
        <ol>
          <% links.each do |name, link, locale| %>
            <li><a href="<%= link %>.html" target="_blank"><%= name %></a></li>
          <% end %>
        </ol>
      <% end %>
    </div>
  </body>
</html>
